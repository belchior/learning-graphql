type Query {
  user(login: String!): User
}

type User implements Node {
  avatarUrl: String!
  bio: String
  company: String
  email: String!
  id: ID!
  location: String
  login: String!
  name: String
  url: String!
  websiteUrl: String
}

interface Node {
  id: ID!
}
